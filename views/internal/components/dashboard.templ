package components

templ DashboardHeader(heading, context string) {
	<div class="flex justify-between items-center mb-8 pb-4 border-b border-base-300">
		<h1 class="text-base-content text-3xl font-semibold m-0">{ heading }</h1>
		<div class="text-base-content text-sm">
			{ context }
		</div>
	</div>
}
