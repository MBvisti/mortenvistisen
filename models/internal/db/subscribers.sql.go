// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: subscribers.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const countSubscribers = `-- name: CountSubscribers :one
select count(*) from subscribers
`

// CountSubscribers
//
//	select count(*) from subscribers
func (q *Queries) CountSubscribers(ctx context.Context, db DBTX) (int64, error) {
	row := db.QueryRow(ctx, countSubscribers)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const deleteSubscriber = `-- name: DeleteSubscriber :exec
delete from subscribers where id=$1
`

// DeleteSubscriber
//
//	delete from subscribers where id=$1
func (q *Queries) DeleteSubscriber(ctx context.Context, db DBTX, id int32) error {
	_, err := db.Exec(ctx, deleteSubscriber, id)
	return err
}

const insertSubscriber = `-- name: InsertSubscriber :one
insert into
    subscribers (created_at, updated_at, email, subscribed_at, referer, is_verified)
values
    (now(), now(), $1, $2, $3, $4)
returning id, created_at, updated_at, email, subscribed_at, referer, is_verified
`

type InsertSubscriberParams struct {
	Email        pgtype.Text
	SubscribedAt pgtype.Timestamptz
	Referer      pgtype.Text
	IsVerified   pgtype.Bool
}

// InsertSubscriber
//
//	insert into
//	    subscribers (created_at, updated_at, email, subscribed_at, referer, is_verified)
//	values
//	    (now(), now(), $1, $2, $3, $4)
//	returning id, created_at, updated_at, email, subscribed_at, referer, is_verified
func (q *Queries) InsertSubscriber(ctx context.Context, db DBTX, arg InsertSubscriberParams) (Subscriber, error) {
	row := db.QueryRow(ctx, insertSubscriber,
		arg.Email,
		arg.SubscribedAt,
		arg.Referer,
		arg.IsVerified,
	)
	var i Subscriber
	err := row.Scan(
		&i.ID,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Email,
		&i.SubscribedAt,
		&i.Referer,
		&i.IsVerified,
	)
	return i, err
}

const queryPaginatedSubscribers = `-- name: QueryPaginatedSubscribers :many
select id, created_at, updated_at, email, subscribed_at, referer, is_verified from subscribers
order by created_at desc
limit $2::bigint offset $1::bigint
`

type QueryPaginatedSubscribersParams struct {
	Offset int64
	Limit  int64
}

// QueryPaginatedSubscribers
//
//	select id, created_at, updated_at, email, subscribed_at, referer, is_verified from subscribers
//	order by created_at desc
//	limit $2::bigint offset $1::bigint
func (q *Queries) QueryPaginatedSubscribers(ctx context.Context, db DBTX, arg QueryPaginatedSubscribersParams) ([]Subscriber, error) {
	rows, err := db.Query(ctx, queryPaginatedSubscribers, arg.Offset, arg.Limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Subscriber
	for rows.Next() {
		var i Subscriber
		if err := rows.Scan(
			&i.ID,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.Email,
			&i.SubscribedAt,
			&i.Referer,
			&i.IsVerified,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const querySubscriberByEmail = `-- name: QuerySubscriberByEmail :one
select id, created_at, updated_at, email, subscribed_at, referer, is_verified from subscribers
where lower(email) = lower($1)
order by id desc
limit 1
`

// QuerySubscriberByEmail
//
//	select id, created_at, updated_at, email, subscribed_at, referer, is_verified from subscribers
//	where lower(email) = lower($1)
//	order by id desc
//	limit 1
func (q *Queries) QuerySubscriberByEmail(ctx context.Context, db DBTX, lower string) (Subscriber, error) {
	row := db.QueryRow(ctx, querySubscriberByEmail, lower)
	var i Subscriber
	err := row.Scan(
		&i.ID,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Email,
		&i.SubscribedAt,
		&i.Referer,
		&i.IsVerified,
	)
	return i, err
}

const querySubscriberByID = `-- name: QuerySubscriberByID :one
select id, created_at, updated_at, email, subscribed_at, referer, is_verified from subscribers where id=$1
`

// QuerySubscriberByID
//
//	select id, created_at, updated_at, email, subscribed_at, referer, is_verified from subscribers where id=$1
func (q *Queries) QuerySubscriberByID(ctx context.Context, db DBTX, id int32) (Subscriber, error) {
	row := db.QueryRow(ctx, querySubscriberByID, id)
	var i Subscriber
	err := row.Scan(
		&i.ID,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Email,
		&i.SubscribedAt,
		&i.Referer,
		&i.IsVerified,
	)
	return i, err
}

const querySubscribers = `-- name: QuerySubscribers :many
select id, created_at, updated_at, email, subscribed_at, referer, is_verified from subscribers
`

// QuerySubscribers
//
//	select id, created_at, updated_at, email, subscribed_at, referer, is_verified from subscribers
func (q *Queries) QuerySubscribers(ctx context.Context, db DBTX) ([]Subscriber, error) {
	rows, err := db.Query(ctx, querySubscribers)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Subscriber
	for rows.Next() {
		var i Subscriber
		if err := rows.Scan(
			&i.ID,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.Email,
			&i.SubscribedAt,
			&i.Referer,
			&i.IsVerified,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateSubscriber = `-- name: UpdateSubscriber :one
update subscribers
    set updated_at=now(), email=$2, subscribed_at=$3, referer=$4, is_verified=$5
where id = $1
returning id, created_at, updated_at, email, subscribed_at, referer, is_verified
`

type UpdateSubscriberParams struct {
	ID           int32
	Email        pgtype.Text
	SubscribedAt pgtype.Timestamptz
	Referer      pgtype.Text
	IsVerified   pgtype.Bool
}

// UpdateSubscriber
//
//	update subscribers
//	    set updated_at=now(), email=$2, subscribed_at=$3, referer=$4, is_verified=$5
//	where id = $1
//	returning id, created_at, updated_at, email, subscribed_at, referer, is_verified
func (q *Queries) UpdateSubscriber(ctx context.Context, db DBTX, arg UpdateSubscriberParams) (Subscriber, error) {
	row := db.QueryRow(ctx, updateSubscriber,
		arg.ID,
		arg.Email,
		arg.SubscribedAt,
		arg.Referer,
		arg.IsVerified,
	)
	var i Subscriber
	err := row.Scan(
		&i.ID,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Email,
		&i.SubscribedAt,
		&i.Referer,
		&i.IsVerified,
	)
	return i, err
}

const upsertSubscriber = `-- name: UpsertSubscriber :one
insert into
    subscribers (created_at, updated_at, email, subscribed_at, referer, is_verified)
values
    (now(), now(), $1, $2, $3, $4)
on conflict (id) do update set updated_at=now(), email=excluded.email, subscribed_at=excluded.subscribed_at, referer=excluded.referer, is_verified=excluded.is_verified
returning id, created_at, updated_at, email, subscribed_at, referer, is_verified
`

type UpsertSubscriberParams struct {
	Email        pgtype.Text
	SubscribedAt pgtype.Timestamptz
	Referer      pgtype.Text
	IsVerified   pgtype.Bool
}

// UpsertSubscriber
//
//	insert into
//	    subscribers (created_at, updated_at, email, subscribed_at, referer, is_verified)
//	values
//	    (now(), now(), $1, $2, $3, $4)
//	on conflict (id) do update set updated_at=now(), email=excluded.email, subscribed_at=excluded.subscribed_at, referer=excluded.referer, is_verified=excluded.is_verified
//	returning id, created_at, updated_at, email, subscribed_at, referer, is_verified
func (q *Queries) UpsertSubscriber(ctx context.Context, db DBTX, arg UpsertSubscriberParams) (Subscriber, error) {
	row := db.QueryRow(ctx, upsertSubscriber,
		arg.Email,
		arg.SubscribedAt,
		arg.Referer,
		arg.IsVerified,
	)
	var i Subscriber
	err := row.Scan(
		&i.ID,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Email,
		&i.SubscribedAt,
		&i.Referer,
		&i.IsVerified,
	)
	return i, err
}
